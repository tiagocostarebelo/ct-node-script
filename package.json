{
  "name": "stage-4",
  "version": "1.0.0",
  "description": "Create a node script which, when executed, reads from the reddit endpoint https://www.reddit.com/r/aww.json and lists out all the post titles to the command line, Each on a new line. It should also output the urls to a text file named 'url_list.log'.",
  "main": "app.js",
  "scripts": {
    "start": "node app",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tiago Costa Rebelo",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.26.0"
  }
}
